package league.types;

public class SimplePlayer extends PlayerBase{
    public SimplePlayer(int playerId, int teamId, String firstName, String lastName, String teamName) {
        super(playerId, teamId, firstName, lastName, teamName);
    }
}
